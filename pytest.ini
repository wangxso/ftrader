[pytest]
# pytest 配置文件
testpaths = src/ftrader/tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
addopts = 
    -v
    --tb=short
    --strict-markers
# 在导入测试之前，先导入 mock 设置
pythonpath = src

